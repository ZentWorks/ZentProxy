(() => {
  const articles = [{"id":"getting-started","category":"Getting Started","en":{"title":"Getting Started","summary":"The basic ZentProxy workflow from first login to the first proxy host.","sections":[["How ZentProxy works",["You configure hosts in the WebUI. ZentProxy validates the settings and applies the configuration automatically.","The default installation stores configuration and statistics in SQLite under /data, so no separate database is required."]],["First proxy host",["Create a Proxy Host, enter one or more domains or IP addresses, select http or https for the upstream, and enter the target host and port. Enable a certificate only when HTTPS should be served by ZentProxy."],["A domain or IP can only belong to one proxy host.","Analytics can be enabled or disabled per host.","Use Trusted Proxy when traffic arrives through Cloudflare or another supported reverse proxy."]],["Recommended first steps",["Create or import your proxy hosts.","Configure certificates for HTTPS hosts.","On each Proxy Host that is reached through Cloudflare, select Cloudflare as the Trusted Proxy Provider in the General tab.","Verify Analytics and then review the Audit Log after configuration changes."]]]}},{"id":"proxy-hosts","category":"Proxy Hosts & Routing","en":{"title":"Proxy Hosts & Routing","summary":"Domains, upstreams, WebSockets, custom locations and advanced OpenResty configuration.","sections":[["Proxy hosts",["A proxy host maps one or more incoming domains or IP addresses to an upstream application. The upstream can use HTTP or HTTPS and may be addressed by Docker name, hostname or IP."]],["WebSockets and Host header",["Enable WebSockets for applications that use connection upgrades. Preserve Host forwards the original incoming hostname instead of replacing it with the upstream hostname."]],["Custom locations",["Custom locations route selected URL paths to a different upstream. They are useful when one public hostname needs to serve multiple internal services."]],["Advanced configuration",["Advanced OpenResty directives are intentionally separated from the common settings. Use them only when a native ZentProxy option does not cover the requirement. Imported directives are preserved."]]]}},{"id":"certificates","category":"SSL & Certificates","en":{"title":"SSL & Certificates","summary":"TLS certificates, Force HTTPS, HTTP/2, HSTS and imported PEM material.","sections":[["Certificate types",["ZentProxy supports Let’s Encrypt certificates and imported PEM certificates. A certificate consists of a certificate chain and its matching private key. The display name and description are ZentProxy metadata and can be edited without replacing the certificate."]],["Host TLS settings",["Assign a certificate to a proxy host to enable HTTPS. Force HTTPS redirects plain HTTP to HTTPS. HTTP/2 can be enabled for TLS listeners."]],["HSTS",["HSTS tells browsers to use HTTPS for future requests. Enable it only after HTTPS works reliably. HSTS Subdomains extends the policy to subdomains and should be used carefully."]],["Imported certificates",["Custom PEM certificates are copied into ZentProxy-managed storage. ZentProxy cannot automatically renew arbitrary custom certificates unless a future provider integration explicitly supports it."]]]}},{"id":"letsencrypt","category":"Let’s Encrypt","en":{"title":"Let’s Encrypt","summary":"HTTP-01, DNS-01, wildcard certificates and automatic renewal.","sections":[["HTTP-01",["HTTP-01 is the simplest challenge. The public domain must resolve to ZentProxy and port 80 must be reachable from the Internet while the certificate is issued or renewed."]],["DNS-01",["DNS-01 proves domain ownership through DNS records. It is required for wildcard certificates and works without exposing port 80, but ZentProxy needs valid API credentials for the DNS provider."]],["Automatic renewal",["Managed Let’s Encrypt certificates are renewed automatically before expiry. A failed renewal is stored as certificate status information so it can be diagnosed in the WebUI."]],["Migration fallback",["If an existing Let’s Encrypt certificate cannot be copied during migration but its ACME configuration is usable, ZentProxy can reissue the certificate automatically."]]]}},{"id":"analytics","category":"Analytics","en":{"title":"Analytics","summary":"Per-host statistics, request details, retention and privacy-related options.","sections":[["Per-host analytics",["Analytics is controlled per proxy host. Disabled hosts are still proxied normally but their requests are not stored in the analytics database."]],["Stored request data",["Statistics include host, canonical client IP, path, method, status, traffic and timing data. Query strings are a separate option because URLs may contain tokens or other sensitive information."]],["Views and filtering",["Use the range selector for 1h, 24h, 7d or 30d. Filter by an exact domain, click a Top Domain to drill into it, and use the visible Auto Refresh switch to control live updates."]],["Real client IP",["When a CDN or reverse proxy is in front of ZentProxy, select a Trusted Proxy Provider on that Proxy Host. Otherwise statistics, rate decisions and ZentLoop may see the proxy IP instead of the real visitor."]],["Retention",["Raw request retention is intentionally finite. Aggregated statistics can be kept longer in future versions without retaining every individual request indefinitely."]]]}},{"id":"trusted-proxies","category":"Trusted Proxies & Cloudflare","en":{"title":"Trusted Proxies & Cloudflare","summary":"Restore real client IPs safely when traffic arrives through Cloudflare or another proxy.","sections":[["Why trusted proxies matter",["A reverse proxy or CDN connects to ZentProxy on behalf of the visitor. Without trusted-proxy handling, the apparent client IP is the proxy address."]],["Cloudflare",["Select Cloudflare for hosts that are proxied through Cloudflare. ZentProxy uses CF-Connecting-IP as the canonical client address only for hosts that explicitly select the Cloudflare provider."]],["Direct traffic on Docker Desktop",["With Direct / None, ZentProxy does not trust forwarded client-IP headers. Docker Desktop for macOS can replace the original source address before the connection reaches the container, commonly exposing a gateway address such as 192.168.65.1. In that case the original visitor IP is no longer available to ZentProxy, so Analytics and forwarded X-Real-IP/X-Forwarded-For values may show that gateway address. ZentProxy does not guess or trust arbitrary headers to work around this."]],["Do not trust everyone",["Never accept forwarded client-IP headers from arbitrary Internet addresses. Attackers can forge those headers unless the sender is verified as a trusted proxy."]]]}},{"id":"access-routing","category":"Access Lists & Streams","en":{"title":"Access Lists & Streams","summary":"Allow/deny rules, Basic Auth, redirects, 404 hosts and TCP/UDP streams.","sections":[["Access lists",["Access lists combine separate IP/CIDR allow and deny lists with optional username/password protection. You can use IP rules only, login credentials only, require both, or allow either one to grant access. Credentials are managed directly in ZentProxy and stored as bcrypt htpasswd hashes. Assign a list to proxy hosts that should not be publicly accessible without restrictions."]],["Redirect and 404 hosts",["Redirect hosts send incoming requests to another URL. 404 hosts intentionally terminate matching hostnames without forwarding to an application."]],["Routing management",["Redirect hosts, explicit 404 hosts and TCP/UDP streams are managed on the Routing page. Click an existing row to edit it or use the add action in each section."]],["Streams",["Streams proxy raw TCP and/or UDP traffic. The incoming port must also be published by Docker or Unraid; ZentProxy cannot add a new host port mapping to an already running container."]]]}},{"id":"migration","category":"Migration","en":{"title":"Migration","summary":"Analyze a running source installation and transfer supported configuration safely.","sections":[["Analyze first",["Migration analysis is read-only. ZentProxy inspects hosts, certificates, access lists, redirects, 404 hosts and streams before writing anything locally."]],["Full migration",["A full migration imports compatible routing, access and TLS settings. Managed Let’s Encrypt certificates may be reissued when safe. If a Let’s Encrypt reissue fails during the import, ZentProxy continues with the remaining migration and leaves dependent routing objects without TLS until the certificate is successfully issued and attached."]],["Blocking conditions",["ZentProxy blocks the full migration when continuing would create a broken setup, for example when a DNS-01 certificate needs credentials that cannot be preserved safely."]],["Source remains unchanged",["The migration source is never modified. Keep the old proxy available until imported hosts and certificates have been tested on ZentProxy."]]]}},{"id":"zentloop","category":"ZentLoop Integration","en":{"title":"ZentLoop Integration","summary":"Connect ZentProxy to ZentLoop for catch-all and deception workflows.","sections":[["Purpose",["ZentLoop is integrated through a proxy-neutral protocol instead of being compiled into the proxy core. This keeps both projects independently usable."]],["Catch-all routing",["ZentProxy automatically derives known root domains from configured Proxy Hosts. Unconfigured subdomains inside those domains can use the ZentLoop catch-all. Hostnames outside all known root domains reach ZentLoop only when the Unknown/Fake Hosts toggle is enabled."]],["Signed ingress",["An optional shared secret signs integration metadata with HMAC-SHA256. ZentProxy also performs a signed integration check so a wrong or outdated secret is visible as a mismatch before traffic depends on it."]],["Health and fallback",["ZentProxy checks ZentLoop every 15 seconds and shows reachability, signature state, latency and errors. If the integration is unavailable or a configured signature cannot be verified, routing fails closed with the selected 403 or 503 fallback instead of hanging."]]]}},{"id":"developer-api","category":"Developer API & Integrations","en":{"title":"Developer API & Integrations","summary":"Versioned REST API and scoped API keys for integrations.","sections":[["API first",["The ZentProxy WebUI uses the same versioned backend API that is available to integrations. The stable base path is /api/v1."]],["API keys",["Create separate API keys for integrations and grant only the scopes they need. API keys are revocable and cannot create additional API keys."]]]}},{"id":"troubleshooting","category":"Troubleshooting","en":{"title":"Troubleshooting","summary":"Common checks when a host, certificate or migration does not behave as expected.","sections":[["Host does not respond",["Verify that the public DNS points to ZentProxy, that Docker/Unraid publishes the expected HTTP/HTTPS ports, and that the upstream host and port are reachable from inside the ZentProxy container."]],["Let’s Encrypt fails",["For HTTP-01, verify public port 80 and DNS resolution. For DNS-01, verify the provider name and credentials. Check the certificate status for the latest ACME error."]],["Wrong client IP",["If a host is behind Cloudflare, configure Cloudflare as its Trusted Proxy Provider. With Direct / None on Docker Desktop for macOS, a gateway address such as 192.168.65.1 can be expected because Docker may hide the original source IP before ZentProxy receives the connection."]],["Migration warning",["Orange migration messages are warnings or planned fallbacks. Red blocking messages mean ZentProxy cannot guarantee a working result and therefore refuses the full import until the dependency is resolved."]]]}}];
  window.ZentDocs = {
    articles,
    get(id, lang) {
      const article = articles.find((x) => x.id === id);
      if (!article) return null;
      const localized = window.ZentI18n?.getDocs?.(lang)?.[id];
      return { ...article, ...(localized || article.en) };
    },
  };
})();
